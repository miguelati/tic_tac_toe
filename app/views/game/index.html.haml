.game{'ng-controller' => 'TicTacToeCtrl'}
  .board
    %img{'ng-repeat' => 'space in spaces', 'ng-src' => "images/#{asset_path('blank.gif')}", 'ng-click' => 'mark($index)', class: "space{{$index}}"}
  .game
    {{status}}
    %br
    =link_to 'reiniciar', root_path